import io.dkozak.kotlin.embedded.scripts.model.Coffee

val coffeeList = listOf(
        Coffee("Cafe Late", 3.0),
        Coffee("Cappuccino", 2.5),
        Coffee("Espresso", 1.0)
)

// return the list in the end
coffeeList